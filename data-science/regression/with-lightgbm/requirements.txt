# ==============================================================================================
# Estimation du prix de vente immobilier (AVM) avec LightGBM — dépendances runtime
# ------------------------------------------------------------------------------------------------
# Installation : `pip install -r requirements.txt`  (ou `make install`)
# La source de vérité pour les métadonnées reste `pyproject.toml` ; ce fichier est fourni pour
# les environnements qui ne peuvent pas installer le projet en mode éditable.
# ==============================================================================================
hydra-core>=1.3
omegaconf>=2.3
pandera[pandas]>=0.20
pydantic>=2.5
pyyaml>=6.0
loguru>=0.7
tqdm>=4.66
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
scikit-learn>=1.4
lightgbm>=4.3
joblib>=1.3
